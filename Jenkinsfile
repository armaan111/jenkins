pipeline {
   agent any // to Deploy on Node 

   stages {
      stage('BUILD STAGE ') {
         steps {
            echo 'Hello we are testing the Jenkins CI Pipeline changes'
         }
      }
   }
}
